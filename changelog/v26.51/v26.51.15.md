# v26.51.15 - 2026-06-09

## 前端

### 测试
- **测试覆盖率 76.88% → 81.09% Statements / 83.48% Lines**：新增约 700 个前端单元测试，29 个文件变更
  - 覆盖率指标：Statements 76.88%→81.09%，Lines 79.49%→83.48%，Branches 71.82%→76.99%，Functions 70.46%→74.72%

### 修复
- **修复 5 个失败测试文件**（22 个测试用例）：
  - `CaseNumberSection.test.tsx`：Dialog/AlertDialog mock 改为条件渲染，避免重复 DOM 元素
  - `OcrUploader.test.tsx`：DropZone mock 补全 `onDrop`/`onDragEnter`/`onDragLeave`/`onDragOver` 事件转发
  - `CaseFolderSection.test.tsx`：toast 断言从英文 `'Scan failed'` 修正为中文 `'扫描失败'`
  - `LprCalculatorTool.test.tsx`：移除多余的 `mockLprCalculate.mockReturnValue()` 覆盖，保持 `mockMutate` 引用一致
  - `IdentityDocManager.test.tsx`：Dialog/AlertDialog mock 条件渲染 + `require('sonner')` 改为顶层 `import` + 禁用按钮断言改为 `toBeDisabled()`

### 新增测试
- **CaseForm.test.tsx**：56 tests（37.2%→90.2%），覆盖创建/编辑模式、表单提交、验证、pending 状态、动态列表
- **CaseMaterialSection.test.tsx**：47 tests（全新文件），覆盖渲染、展开折叠、上传、重命名、删除、绑定对话框
- **CaseDetail.test.tsx**：+22 tests，覆盖 delete 流程、CaseStatusBadge 分支、Party/Lawyer 详情面板、平台标签
- **CourtGuaranteeSection.test.tsx**：+32 tests，覆盖询价、轮询、绑定、重试、删除、格式化分支
- **ContractDetail.test.tsx**：+24 tests，覆盖 handleDelete、handleAction 各分支、Party/Lawyer 详情面板
- **ServiceConfig.test.tsx**：+16 tests（72.6%→84%），覆盖 mutation 回调、创建/编辑/删除对话框、系统分类、测试连接
- **routes/index.tsx**：79 tests（替换旧 37 tests），覆盖路由结构、守卫、顺序、唯一性验证
- **Sidebar.test.tsx**：+45 tests（62.1%→80%），覆盖自动展开、折叠弹窗、子菜单交互、底部菜单活跃状态
- **MarkdownContent.test.tsx**：+18 tests（61.5%→82.4%），覆盖代码块复制、preprocessContent、流式渲染
- **CaseFolderSection.test.tsx**：+24 tests，覆盖扫描成功/失败、候选选择、导入流程、错误处理
- **ElementConvertTool.test.tsx**：+15 tests，覆盖转换成功/失败、文件名重命名、错误分支
- **BatchDownloadButton.test.tsx**：+10 tests，覆盖 CSV/ZIP 下载、404 处理、加载状态
- **CaseSearchSelect.test.tsx**：新增 tests，覆盖搜索、选择、防抖、加载、错误状态
- **RecognitionList.test.tsx**：新增 tests，覆盖列表渲染、过滤、分页、状态显示
- **BatchHistoryPanel.test.tsx**：+34 tests，覆盖状态徽章、下载按钮、折叠详情、错误消息
- **LawFirmForm.test.tsx**：+14 tests，覆盖创建/编辑模式、加载/错误状态、字段标签
- **ReminderForm.test.tsx**：+51 tests，覆盖表单渲染、验证、提交、日期选择、错误状态
- **TemplateList.test.tsx**：+47 tests，覆盖列表渲染、过滤、搜索、空状态、加载状态
- **ClientForm.test.tsx**：+17 tests，覆盖创建/编辑模式、客户类型切换、表单提交
- **LogsPage.test.tsx**：新增 tests，覆盖过滤、分页、搜索、错误状态

### 依赖
- **新增 `@vitest/coverage-v8@4.1.2`**：显式添加到 devDependencies，修复 vitest 与 coverage 插件版本不匹配警告
